identity:
  name: tgai_kv_get_str
  author: Enty
  label:
    en_US: TGAI KV GET STRING
    zh_Hans: TGAI KV GET STRING
    pt_BR: TGAI KV GET STRING
description:
  human:
    en_US: A tool for get value from kv store with key, result type is string.
    zh_Hans: 一个用于从 TGAI KV 数据库中根据指定 key 获取 value 的工具，返回结果为 string
    pt_BR: A tool for get value from kv store with key, result type is string.
  llm: A tool for get value from kv store with key, result type is string.
parameters:
  - name: key
    type: string
    required: true
    label:
      en_US: key
      zh_Hans: key
      pt_BR: key
    human_description:
      en_US: key used for querying from KV DB
      zh_Hans: 要查询的 key
      pt_BR:  key used for querying from KV DB
    llm_description: key used for querying from KV DB
    form: llm
